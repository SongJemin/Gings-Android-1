<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.computer.inu.myworkinggings">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".SignUp1Activity" />
        <activity android:name=".SignUp2Activity" />
        <activity android:name=".SignUp3Activity" />
        <activity android:name=".Hyunjin.Activity.MessageSend1Activity" />

        <activity android:name=".BottomNaviActivity"></activity>

        <activity android:name=".UpBoardActivity"
            android:windowSoftInputMode="adjustResize">

        </activity>


        <activity android:name=".DetailBoardActivity"></activity>

    </application>

</manifest>